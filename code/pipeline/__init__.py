"""Deterministic data-loading and schema utilities for the HackerRank
Orchestrate multi-modal evidence review pipeline.

Sub-modules:
    data    -- CSV loading for claims, user history, evidence requirements
    images  -- image resolution, format sniffing, AVIF transcoding, base64 encoding
    schema  -- output schema constants, row assembly, and validation
"""
